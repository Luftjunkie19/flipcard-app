export class LanguageSet {
  constructor(name, flagImage, id, ownerId, flipCardsets) {
    this.name = name;
    this.flipCardsets = flipCardsets;
    this.flagImage = flagImage;
    this.id = id;
    this.ownerId = ownerId;
  }
}
